{
  "name": "performos-frontend",
  "build": {
    "type": "nodejs",
    "buildCommand": "yarn install && yarn build",
    "outputDirectory": "build"
  },
  "run": {
    "command": "npx serve -s build -l ${PORT:-3000}"
  },
  "env": {
    "REACT_APP_BACKEND_URL": "${REACT_APP_BACKEND_URL}"
  }
}